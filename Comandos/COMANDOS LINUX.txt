pwd // en que dir estoy

ls -alrt // trae el contenido del dir actual

tree *DIRECTORIO* -L directorio a buscar //permite buscar niveles en un directorio

grep CADENA *DIRECTORIO* *DIRECTORIO* //buscar palabra en una cadena con todo
grep -w CADENA *DIRECTORIO* //busca palabra completa teniendo en cuenta mayus y min
grep -i CADENA *DIRECTORIO* //busca la palabra ignorando mayusculas y minusculas
grep -r CADENA *DIRECTORIO* //busca de forma recursiva la palabra
grep -x "PALABRA A BUSCAR" *DIRECTORIO* //busca la palabra exacta teniendo en cuenta mayus y min
grep -c Cadena|"PALABRA A BUSCAR"(palabra especifica) *DIRECTORIO* //contar la palabra en directorio
grep -l CADENA *DIRECTORIO* // devuelve los archivos donde se encuentra la palabra
grep -n CADENA *DIRECTORIO* // devuelve la liena donde se encuentra la cadena
grep -v CADENA *DIRECTORIO* //devuelve donde aparece la palabra(busqueda inversa)

~ (Ir carpeta de usuario)
. (referencia a la carpeta actual)
.. (carpeta hacia arriba)

sudo apt update //actualizacion de repos
sudo apt list --upgradable // que debemos instalar

man COMANDO //manual del comando ingresado

nano NOMBREARCHIVO.EXTENSION // abrir editor de texto

mkdir NOMBRECARPETA // crear carpeta
 
find DIRECTORIO/ NOMBREARCHIVO // busca archivo en el directorio
find .(referencia a la carpeta actual) NOMBREARCHIVO
find -L DIRECTORIO - name NOMBRE// buscar links simbolicos o acceso directo
find DIRECTORIO -type f // todos los tipo archivo en la ubicacion
find DIRECTORIO -type d // todos los directorios en la ubicacion
find DIRECTORIO -type f -size -TAMAÑO (ejemplo :1K - kilobyte, 1Mc- megas)
find -type f -ntime DIASDESDEMODIFICACION (+5 hace 5 dias)//buscar archivos por fecha de modificacion

ls -l //listar archivos directorio actual forma alfabetica
ls -al //listar todos los archivos incluyendo ocultos
ls -lt // listar archivos del mas reciente al mas viejo
ls -ltr // listar mas viejo al mas reciente